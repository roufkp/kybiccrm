const sidebar = document.querySelector('.sidebar');
const side = document.querySelector('.side');
const navItems = document.querySelectorAll('nav .nav-item');
const toggle = document.querySelector(' .toggle');

toggle.addEventListener('click', () => {

if (side.className === 'side')
side.classList.add('open');
else
side.classList.remove('open');



if (sidebar.className === 'sidebar')
sidebar.classList.add('open');
else
sidebar.classList.remove('open');

});

navItems.forEach(navItem => {

navItem.addEventListener('click', () => {

navItems.forEach(navItem => {
navItem.classList.remove('active');
});

navItem.classList.add('active');

});

});





// const rowsPerPage = 5;

// // Get all tables generated by the loop
// const tables = document.querySelectorAll('.generated-table');

// tables.forEach(table => {
//   const tableRows = table.querySelectorAll('tbody tr');
//   const pageCount = Math.ceil(tableRows.length / rowsPerPage);
 

//   let currentPage = 1;

//   function showPage(page) {
//     const start = (page - 1) * rowsPerPage;
//     const end = start + rowsPerPage;

//     tableRows.forEach((row, index) => {
//       if (index >= start && index < end) {
//         row.style.display = '';
//       } else {
//         row.style.display = 'none';
//       }
//     });
//   }

//   if (tableRows.length === 1) {
//     table.querySelector('.next-btn').style.display = 'none';
//     table.querySelector('.prev-btn').style.display = 'none';
//     table.querySelector('.table-container').innerHTML = '<p>There is nothing to show in this table</p>';
//   } else {
//     table.querySelector('.prev-btn').addEventListener('click', () => {
//       if (currentPage > 1) {
//         currentPage--;
//         showPage(currentPage);
//       }
//     });

//     table.querySelector('.next-btn').addEventListener('click', () => {
//       if (currentPage < pageCount) {
//         currentPage++;
//         showPage(currentPage);
//       }
//     });

//     showPage(currentPage);
//   }
// });
